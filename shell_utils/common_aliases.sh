# Alias without argument
alias rmh="rm -f ~/.ssh/known_hosts"
alias clean_trash="rm -rf ~/.local/share/Trash/*"
alias sshp="sshpass -p"
alias cb="xclip -selection clipboard -in" # Copy to clipboard
alias pb='xclip -selection clipboard -out' # Paste from clipboard
alias mount_h='sudo mount -t drvfs H: /mnt/h'
alias subl='/mnt/c/Program\ Files/Sublime\ Text/subl.exe'